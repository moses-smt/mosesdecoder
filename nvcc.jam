import type ;
import toolset ;

type.register CUDA : cu ;

import generators ;
generators.register-standard nvcc.compile : CUDA : OBJ ;

toolset.flags nvcc.compile INCLUDES <include> ;

actions compile
{
    "/usr/local/cuda/bin/nvcc" -I"$(INCLUDES)" -std=c++11 -O0 -g -o $(<) -c $(>)
}
